<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no"><!-- Required meta tags -->

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link rel="stylesheet" href="../public/CSS/Cart_CSS.css">
    <link rel="stylesheet" type="text/css" href="../public/CSS/Home_CSS.css" />
    <title>Shopping Cart</title>
</head>
<body id="body-start" style="background: grey;">
<%- include("header") -%>
    <div class="container">
        <h2><%= title %></h2>
        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th><input class="form-check-input mt-0" type="checkbox" name="" id="" ></th>
                    <th>Product</th>
                    <th>Name</th>
                    <th>Artist</th>
                    <th>Price</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <% cart.forEach(function (cart) { %>
                    <% let username = "lm021804" %>
                    <% if (cart.username == username) { %>
                        <% for (let i = 0; i < cart.artNames.length; i++) { %>
                            <tr>
                                <td><input class="form-check-input mt-0" type="checkbox" name="" id=""></td>
                                <td><img class="image" src="public/Images/Art/<%= cart.artDetails[i].artImage %>" alt="<%= cart.artDetails[i].artImage %>" /></td>
                                <td><%= cart.artDetails[i].artName %></td>
                                <td><%= cart.artDetails[i].artistName %></td>
                                <td><%= cart.artDetails[i].cost %></td>
                                <td><a href="delete/<%= cart.artNames[i] %>" class="btn btn-sm btn-danger">Delete</a></td>
                            </tr>
                        <% } %>
                    <% } %>
                <% }); %>
            </tbody>
        </table>
    </div>

    <%- include("footer") -%>

</html>